{
  "title": "Pricing Latency",
  "panels": [
    {
      "type": "graph",
      "title": "pricing_request_duration_seconds p95/p99",
      "targets": [
        { "expr": "histogram_quantile(0.95, sum(rate(pricing_request_duration_seconds_bucket[5m])) by (le))" },
        { "expr": "histogram_quantile(0.99, sum(rate(pricing_request_duration_seconds_bucket[5m])) by (le))" }
      ]
    }
  ]
}